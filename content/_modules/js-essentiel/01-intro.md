---
title: "JavaScript — juste ce qu'il faut"
type: lesson
---

# Étape 1 — JavaScript essentiel

Avant Vue, il faut le **strict nécessaire** de JavaScript moderne. Pas tout le langage : juste ce qui revient sans arrêt dans un projet Vue.

> **Objectif de l'étape —** être à l'aise avec la syntaxe ES moderne qu'on utilise dans chaque composant Vue.

## Au programme

- `let` / `const`, portée de bloc
- Fonctions fléchées et `this`
- Destructuration (objets, tableaux), spread `...`
- Modules `import` / `export`
- Tableaux : `map`, `filter`, `reduce`, `find`
- Promesses et `async` / `await`
- Optional chaining `?.` et nullish `??`

*(Contenu détaillé à venir — ceci est le squelette de l'étape.)*
